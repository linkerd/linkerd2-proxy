[package]
name = "linkerd2-app"
version = "0.1.0"
authors = ["Linkerd Developers <cncf-linkerd-dev@lists.cncf.io>"]
license = "Apache-2.0"
edition = "2018"
publish = false
description = """
Configures and executes the proxy

This is used by tests and the executable.
"""

[features]
mock-orig-dst  = ["linkerd2-app-core/mock-orig-dst"]

[dependencies]
futures = "0.3"
indexmap = "1.0"
ipnet = "2.0"
linkerd2-app-core = { version = "0.1", path = "./core" }
linkerd2-app-gateway = { version = "0.1", path = "./gateway" }
linkerd2-app-inbound = { version = "0.1", path = "./inbound" }
linkerd2-app-outbound = { version = "0.1", path = "./outbound" }
linkerd2-channel = { version = "0.1", path = "../channel" }
linkerd2-opencensus = { version = "0.1", path = "../opencensus" }
linkerd2-error = { version = "0.1", path = "../error" }
regex = "1.0.0"
tokio = { version = "1", features = ["rt"] }
tonic = { version = "0.3", default-features = false, features = ["prost"] }
tower = "0.4"
tracing = "0.1.22"
tracing-futures = { version = "0.2", features = ["std-future"]}
