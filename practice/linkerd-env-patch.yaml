# linkerd-env-patch.yaml
spec:
  template:
    spec:
      containers:
      - name: linkerd-proxy
        image: 10.12.0.106:5000/linkerd-proxy:myntra1.0
        # Use strategic-merge to add to existing fields (like envFrom) 
        # without removing other definitions (like image, resources, etc.)
        envFrom:
        - configMapRef:
            name: myntranfrglobalheader
